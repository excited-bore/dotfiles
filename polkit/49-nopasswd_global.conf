# Allow members of the wheel group to execute any actions
#  without password authentication, similar to "sudo NOPASSWD:"
 
Identity=unix-group:wheel;unix-group:sudo
ResultActive=yes
ResultInactive=yes
